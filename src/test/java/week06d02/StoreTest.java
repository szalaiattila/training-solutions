package week06d02;

import java.util.Arrays;

import static org.junit.jupiter.api.Assertions.*;

public class StoreTest {

    Store store = new Store(Arrays.asList(
            new Product name "Kifli". Category, BAKE

}